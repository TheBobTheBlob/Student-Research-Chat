{
    "name": "tests",
    "version": "1.0.0",
    "description": "Testing suite for student-research-chat application",
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.57.0",
        "@types/node": "^25.0.1"
    }
}
