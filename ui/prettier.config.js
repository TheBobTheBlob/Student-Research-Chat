//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: false,
    singleQuote: false,
    trailingComma: "all",
    tabWidth: 4,
    printWidth: 140,
}

export default config
